// Using Javascript Map array method
// map() method is used for creating a new array from an existing one

const numbers = [1, 2, 3, 4];
const doubled = numbers.map(item => item * 2);
console.log(doubled); // [2, 4, 6, 8]
